/**
 * Placeholder types. Once the migrations in supabase/migrations have
 * been applied to your project, replace this file by running:
 *
 *   supabase gen types typescript --project-id <your-project-id> > src/types/database.ts
 *
 * Until then this is typed loosely so the rest of the app compiles.
 */
export type Database = Record<string, unknown>
